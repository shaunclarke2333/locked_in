pipeline {

    agent any

    stages {
        
        stage("build") {
            
            steps {
                echo "Starting build stage: Building docker image"
            }
        }
        stage("test") {

            steps {
                echo "Testing the docker image"
            }
        }
        stage("deploy") {

            steps {
                echo "Deployign the Docker image"
            }
        }
    }
}